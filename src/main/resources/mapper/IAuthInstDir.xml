<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iAuthInstDirDao">
    <select id="selectAuthOrgViewByInstCode"  resultType="authInstDirEntity" parameterType="java.lang.String">
        SELECT * FROM auth_org_view WHERE instCode=#{instCode}
    </select>

    <insert id="insertAuthInstDir" parameterType="authInstDirEntity">
        insert into auth_org_view (instType,instTypeOrder,ratifyDateOrder,mainInstId,instCode,instName,instStatus,address,districtCode,districtCodeName,principal,ratifyDate,validityDate,contactPeroson,tel,cccBm,cccName) values(#{instType},#{instTypeOrder},#{ratifyDateOrder},#{mainInstId},#{instCode},#{instName},#{instStatus},#{address},#{districtCode},#{districtCodeName},#{principal},#{ratifyDate},#{validityDate},#{contactPeroson},#{tel},#{cccBm},#{cccName})
    </insert>

</mapper>